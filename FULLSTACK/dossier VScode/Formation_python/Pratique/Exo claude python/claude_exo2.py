#ici, on apprend if | elif | else
#cas 1
mana  = 30.0
cout_sort = 3.0
if mana >= cout_sort:
    mana_restant = mana - cout_sort
    print(f"Sort lancé ! Mana restant : {mana_restant}")
else:
    print("à court de mana")

#cas 2

mana = 2.9
if mana >= cout_sort:
    mana_restant = mana - cout_sort
    print(f"Sort lancé ! Mana restant : {mana_restant}")
else:
    print("à court de mana")